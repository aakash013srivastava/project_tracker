<%if session[:username].present? && session[:user_id].present? %>
<h2>Welcome <%= p session[:username]%></h2>
<% else %>
<%=link_to("Register",{:controller => 'users',:action => 'new'})%> OR
<%=link_to("Login",{:controller => 'access',:action => 'login'})%>
<%end%>
<h2>This is Users index page</h2>
